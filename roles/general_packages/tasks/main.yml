---
- name: Install general package.
  ansible.builtin.apt:
    pkg:
      - vim
      - telnet
      - net-tools
      - dnsutils
      - mlocate
      - git
      - apt-transport-https
      - wget
    state: latest
