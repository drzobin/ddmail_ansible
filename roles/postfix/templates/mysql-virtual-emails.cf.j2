user = {{ mail_db_user_r }}
password = {{ mail_db_user_r_password }}
hosts = 127.0.0.1
dbname = {{ mail_db_name }}
#query = SELECT 1 FROM emails WHERE email='%s'
#query = SELECT 1 FROM emails left join accounts on emails.account_id = accounts.id WHERE accounts.is_enabled = 1 and email = '%s'
query = SELECT email FROM emails left join accounts on emails.account_id = accounts.id WHERE accounts.is_enabled = 1 and email = '%s'
